## Designer Pro Backend

### Setup

1. Install dependencies:
   `npm install`
2. Verify `.env` values in this folder.

### Run

```bash
npm run dev
```

### Default Login (from `.env`)

- Email: `admin@designerpro.com`
- Password: `admin123`
